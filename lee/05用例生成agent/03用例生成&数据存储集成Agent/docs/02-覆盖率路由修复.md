# P0-2 覆盖率路由修复

> **状态**：✅ 已确定
> **优先级**：P0（必修）
> **影响文件**：`workflow/case_generator.py`

---

## 1. 问题定位

`workflow/case_generator.py:190`：

```python
if coverage_check_result.get('coverage') == "100%":  # ❌ 永远 False
    return "保存用例"
else:
    return "补充生成用例"  # ❌ 死循环
```

- 提示词要求 coverage 保留一位小数 → 实际输出是 `"100.0%"`
- 代码期望 `"100%"`（无小数）
- **任意覆盖率都到不了 100%，工作流永远进入"补充生成"死循环**

---

## 2. 修复策略

新增辅助函数 `parse_coverage_percent()`，把字符串百分比解析为 float 后做容错判断（容忍 `99.95%` ~ `100.0%` 算 100%）。

---

## 3. 代码改动

### 3.1 新增工具函数（放在 `case_generator.py` 顶部）

```python
def parse_coverage_percent(coverage_str: str) -> float:
    """把 LLM 输出的覆盖率字符串解析为 0-100 的浮点数。
    支持格式：'100%' / '100.0%' / '75.0%' / '0%' / 异常时返回 0.0
    """
    if not coverage_str:
        return 0.0
    try:
        return float(str(coverage_str).strip().rstrip('%').strip())
    except (ValueError, AttributeError):
        return 0.0
```

### 3.2 改造 `verify_coverage_router`

```python
@staticmethod
def verify_coverage_router(state: State):
    """验证覆盖率的路由"""
    coverage_check_result = state.get('coverage_check_result') or {}
    coverage_value = parse_coverage_percent(coverage_check_result.get('coverage', '0%'))
    # 容忍 99.95% 以上算达标（避免浮点精度问题）
    if coverage_value >= 99.95:
        return "保存用例"
    return "补充生成用例"
```

---

## 4. 验证方法

```python
# quick_test_p0_2.py
from rag_agent.workflow.case_generator import GenerateCaseWorkflow, parse_coverage_percent

# 1. 解析器单元测试
assert parse_coverage_percent("100.0%") == 100.0
assert parse_coverage_percent("100%") == 100.0
assert parse_coverage_percent("75.0%") == 75.0
assert parse_coverage_percent("0%") == 0.0
assert parse_coverage_percent("") == 0.0
assert parse_coverage_percent("invalid") == 0.0
print("解析器 OK")

# 2. 路由函数测试
assert GenerateCaseWorkflow.verify_coverage_router(
    {"coverage_check_result": {"coverage": "100.0%"}}
) == "保存用例"
assert GenerateCaseWorkflow.verify_coverage_router(
    {"coverage_check_result": {"coverage": "100%"}}
) == "保存用例"  # 兼容无小数格式
assert GenerateCaseWorkflow.verify_coverage_router(
    {"coverage_check_result": {"coverage": "75.0%"}}
) == "补充生成用例"
print("路由函数 OK")
```
