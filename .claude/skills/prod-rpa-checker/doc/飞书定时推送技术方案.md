# RPA 数据采集校验报告 — 飞书定时推送技术方案

> 方案选型：**飞书自定义机器人 Webhook（推送） + Windows 任务计划（调度）**
> 编写日期：2026-06-04

---

## 1. 总体架构

```
┌─────────────────────────────────────────────────────────┐
│  调度层：Windows 任务计划程序（与项目解耦）                │
│  每天 08:30 / 17:10 触发 → 直接调用技能的 run_check.py          │
│  不依赖任何特定项目目录结构                                │
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│  技能目录（自包含、可移植）                                │
│  .claude/skills/prod-rpa-checker/                        │
│  ├── SKILL.md              ← 技能描述（Claude Code 识别） │
│  ├── config.yaml           ← 技能运行时配置               │
│  ├── setup_task.ps1        ← 一次性部署脚本（Windows）    │
│  ├── run_check.py          ← 调度入口（技能内）            │
│  ├── scripts/                                            │
│  │   ├── checker.py        ← 校验核心逻辑                 │
│  │   └── feishu_notifier.py ← 飞书推送模块                │
│  ├── output/               ← 报告 + 日志输出              │
│  ├── doc/                                                  │
│  └── sqls/                                                 │
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│  飞书群 → 自定义机器人 → 交互消息卡片                      │
│  📊 RPA数据采集校验报告 — 2026-06-04                      │
│  ✅ 通过: 47  ❌ 失败: 64                                 │
│  [📄 查看完整报告] → wiki 页面                            │
└─────────────────────────────────────────────────────────┘
```

### 解耦设计原则

| 原则             | 实现方式                                                                      |
| ---------------- | ----------------------------------------------------------------------------- |
| **技能自包含**   | `run_check.py` 在技能目录内，通过 `__file__` 自动定位自身路径                 |
| **无项目硬编码** | 不引用 `e:\AI\pythonProject\aiAutoTest` 等绝对路径                            |
| **配置外置**     | Webhook URL、数据库连接等通过 `.env` 或 `config.yaml` 读取                    |
| **可复制即用**   | 整个 `prod-rpa-checker` 目录复制到任何其他项目/智能体，配置好 `.env` 即可运行 |

---

## 2. 飞书 Webhook 配置

### 2.1 创建自定义机器人

1. 打开目标飞书群 → 设置 → 群机器人 → 添加机器人 → **自定义机器人**
2. 填写机器人名称（如 `RPA数据监控`）和头像
3. 获取 **Webhook URL**，格式：
   ```
   https://open.feishu.cn/open-apis/bot/v2/hook/<your-hook-id>
   ```
4. 安全设置（三选一）：
   - **推荐：签名校验** — 获取 `Secret`，脚本中用 HMAC-SHA256 签名
   - 关键词过滤 — 设置消息中必须包含 "RPA" 等关键词
   - IP 白名单 — 限制允许的服务器 IP

### 2.2 存储凭证

在**项目根目录**的 `.env` 中新增（文件已被 git 忽略）：

```env
# .env — 放在项目根目录，与技能目录无关
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
FEISHU_WEBHOOK_SECRET=SECxxxxxxxxxxxxxxxxxxxxx   # 签名校验用，可选
```

> **可移植说明**：技能复制到新项目时，只需在新项目的 `.env` 中配置相同变量即可。

---

## 3. 消息卡片设计

飞书自定义 Webhook 支持发送**交互卡片（interactive）**，比纯文本更美观。

### 3.1 消息体结构

```json
{
  "msg_type": "interactive",
  "card": {
    "config": { "wide_screen_mode": true },
    "header": {
      "title": { "tag": "plain_text", "content": "📊 RPA数据采集校验报告" },
      "template": "red"
    },
    "elements": [
      {
        "tag": "div",
        "text": {
          "tag": "lark_md",
          "content": "**执行时间**：2026-06-04 17:10:28"
        }
      },
      {
        "tag": "div",
        "text": {
          "tag": "lark_md",
          "content": "**✅ 通过**：47  **❌ 失败**：64"
        }
      },
      {
        "tag": "hr"
      },
      {
        "tag": "div",
        "text": {
          "tag": "lark_md",
          "content": "**广东** ❌ 5项失败\n**江苏** ❌ 9项失败\n**浙江** ❌ 2项失败\n..."
        }
      },
      {
        "tag": "action",
        "actions": [
          {
            "tag": "button",
            "text": { "tag": "plain_text", "content": "📄 查看完整报告" },
            "url": "https://wbenergy.feishu.cn/wiki/APocwMagEigGtmkgLKVcHr2Dned",
            "type": "primary"
          }
        ]
      }
    ]
  }
}
```

### 3.2 设计原则

| 元素                | 用途                                                                                    |
| ------------------- | --------------------------------------------------------------------------------------- |
| `header.template`   | `red` = 有失败项，`green` = 全部通过                                                    |
| `div`               | 摘要信息：执行时间、通过/失败统计、按交易中心概要                                       |
| `hr`                | 分隔线                                                                                  |
| `div` + `lark_md`   | 各交易中心失败项摘要（最多展示前 3 条）                                                 |
| `action` + `button` | 跳转到飞书 wiki 报告子页面（每次动态更新，兜底链接：`https://wbenergy.feishu.cn/wiki/APocwMagEigGtmkgLKVcHr2Dned`） |

> **注意**：飞书卡片有长度限制，**不宜将完整 Markdown 报告直接塞入卡片**。
> 策略：卡片内放**摘要 + 统计**，完整报告记录在 wiki 中，按钮一键跳转。

---

## 4. 核心代码设计

### 4.1 目录结构（技能内自包含）

```
.claude/skills/prod-rpa-checker/
├── SKILL.md                          # 现有技能描述（不变）
├── config.yaml                       # 新增：技能运行时配置
├── setup_task.ps1                    # 新增：一次性部署脚本（Windows 任务计划注册）
├── run_check.py                      # 新增：调度入口（技能内，可被外部直接调用）
├── scripts/
│   ├── checker.py                    # 现有校验核心逻辑（改造）
│   ├── feishu_notifier.py            # 新增：飞书推送模块
│   └── wiki_updater.py               # 新增：wiki 文档更新模块
├── output/                           # 现有报告输出目录（不变）
│   └── run_check.log                 # 运行日志
├── doc/
│   ├── 数据中心类型定义.json
│   └── 飞书定时推送技术方案.md
└── sqls/                             # 现有 SQL 模板（不变）
```

### 4.2 config.yaml（新增 — 技能配置外置）

```yaml
# .claude/skills/prod-rpa-checker/config.yaml
# 技能运行时配置，与项目 .env 互补
# .env 存放敏感凭证，config.yaml 存放非敏感参数

# 飞书通知
feishu:
  wiki_url: "https://wbenergy.feishu.cn/wiki/APocwMagEigGtmkgLKVcHr2Dned"
  wiki_parent_node_token: "APocwMagEigGtmkgLKVcHr2Dned"
  wiki_space_id: "7316788734647599106"
  button_text: "📄 查看完整报告"
  last_doc_id: ""          # 上一次创建的文档 ID（用于清理旧报告）
  last_node_token: ""      # 上一次创建的 wiki 子节点 token

# 通知策略
notify:
  mode: "always" # always | on-failure
  max_retries: 3 # 飞书推送重试次数
  retry_backoff: 2 # 指数退避基数（秒）

# 校验参数
checker:
  default_connection: "prod"
  report_dir: "output" # 相对于技能目录

# 日志
logging:
  level: "INFO"
  file: "output/run_check.log"
```

### 4.3 feishu_notifier.py（新增）

```python
"""飞书 Webhook 通知模块 — 将校验结果推送到飞书群"""

import hashlib
import hmac
import base64
import time
import json
import logging
import requests
from pathlib import Path
from datetime import datetime
from typing import Any

logger = logging.getLogger(__name__)

# ---------------------------------------------------------------------------
# 签名工具
# ---------------------------------------------------------------------------

def _gen_sign(timestamp: int, secret: str) -> str:
    """生成飞书 Webhook 签名（HMAC-SHA256）"""
    string_to_sign = f"{timestamp}\n{secret}"
    hmac_code = hmac.new(
        string_to_sign.encode("utf-8"), digestmod=hashlib.sha256
    ).digest()
    return base64.b64encode(hmac_code).decode("utf-8")


# ---------------------------------------------------------------------------
# 卡片构建
# ---------------------------------------------------------------------------

def build_card(
    result: dict[str, Any],
    wiki_url: str = "https://wbenergy.feishu.cn/wiki/APocwMagEigGtmkgLKVcHr2Dned",
    button_text: str = "📄 查看完整报告",
) -> dict:
    """
    从 checker 返回的结构化结果构建飞书消息卡片。

    result 预期结构:
    {
        "exec_time": "2026-06-04 17:10:28",
        "pass_count": 47,
        "fail_count": 64,
        "centers": [
            {"name": "广东", "trade_center_id": 1, "failures": [...], "all_pass": False},
            ...
        ]
    }
    """
    has_failures = result["fail_count"] > 0
    header_template = "red" if has_failures else "green"
    status_emoji = "❌" if has_failures else "✅"

    # 各中心摘要
    center_lines = []
    for c in result["centers"]:
        if c["all_pass"]:
            center_lines.append(f"**{c['name']}** ✅ 全部通过")
        else:
            fail_count = len(c.get("failures", []))
            preview = c["failures"][:3]
            lines = "\n".join(f"  • {f['data_type']}: {f['message']}" for f in preview)
            more = f"\n  • ... 等共 {fail_count} 项失败" if fail_count > 3 else ""
            center_lines.append(f"**{c['name']}** ❌ {fail_count}项失败\n{lines}{more}")

    center_md = "\n\n".join(center_lines)

    card = {
        "config": {"wide_screen_mode": True},
        "header": {
            "title": {"tag": "plain_text", "content": f"{status_emoji} RPA数据采集校验报告"},
            "template": header_template,
        },
        "elements": [
            {
                "tag": "div",
                "text": {"tag": "lark_md", "content": f"**执行时间**：{result['exec_time']}"},
            },
            {
                "tag": "div",
                "text": {
                    "tag": "lark_md",
                    "content": f"**✅ 通过**：{result['pass_count']}  **❌ 失败**：{result['fail_count']}",
                },
            },
            {"tag": "hr"},
            {"tag": "div", "text": {"tag": "lark_md", "content": center_md}},
            {
                "tag": "action",
                "actions": [
                    {
                        "tag": "button",
                        "text": {"tag": "plain_text", "content": button_text},
                        "url": wiki_url,
                        "type": "primary",
                    }
                ],
            },
        ],
    }
    return card


# ---------------------------------------------------------------------------
# 发送逻辑
# ---------------------------------------------------------------------------

def send_to_feishu(
    result: dict[str, Any],
    webhook_url: str,
    secret: str | None = None,
    wiki_url: str = "https://wbenergy.feishu.cn/wiki/APocwMagEigGtmkgLKVcHr2Dned",
    button_text: str = "📄 查看完整报告",
    max_retries: int = 3,
    retry_backoff: int = 2,
) -> bool:
    """
    发送校验结果到飞书。

    :param result: checker 返回的结构化结果
    :param webhook_url: 飞书 Webhook URL
    :param secret: Webhook 签名密钥（可选）
    :param wiki_url: 消息卡片中的跳转链接
    :param button_text: 按钮显示文字
    :param max_retries: 最大重试次数
    :param retry_backoff: 指数退避基数（秒）
    :return: 是否发送成功
    """
    card = build_card(result, wiki_url, button_text)
    payload = {"msg_type": "interactive", "card": card}

    # 签名
    if secret:
        timestamp = int(time.time())
        payload["timestamp"] = str(timestamp)
        payload["sign"] = _gen_sign(timestamp, secret)

    for attempt in range(1, max_retries + 1):
        try:
            resp = requests.post(webhook_url, json=payload, timeout=10)
            resp.raise_for_status()
            data = resp.json()
            if data.get("code") == 0:
                logger.info("飞书推送成功")
                return True
            else:
                logger.warning(f"飞书返回错误 (尝试 {attempt}/{max_retries}): {data}")
        except requests.RequestException as e:
            logger.warning(f"飞书请求异常 (尝试 {attempt}/{max_retries}): {e}")

        if attempt < max_retries:
            backoff = retry_backoff ** attempt  # 2s, 4s, 8s
            time.sleep(backoff)

    logger.error(f"飞书推送失败，已重试 {max_retries} 次")
    return False
```

### 4.4 checker.py 改造

当前 `checker.py` 由 Claude Code skill 驱动（AI 读 JSON → 拼 SQL → 执行 → 拼 Markdown）。
为了支持程序化调用，需要增加**结构化结果返回**能力：

```python
# 在 checker.py 中新增函数

def run_check(connection: str = "prod") -> dict[str, Any]:
    """
    执行完整校验流程，返回结构化结果（而非仅打印 Markdown）。

    返回:
    {
        "exec_time": "2026-06-04 17:10:28",
        "pass_count": int,
        "fail_count": int,
        "centers": [
            {
                "name": "广东",
                "trade_center_id": 1,
                "all_pass": False,
                "failures": [
                    {"data_type": "日前节点电价", "offset": "D",
                     "message": "预期 2026-06-04，实际 2026-06-03"},
                    ...
                ]
            },
            ...
        ],
        "report_file": "output/rpa_check_report_20260604_171028.md"
    }
    """
    # ... 复用现有校验逻辑，在生成 Markdown 的同时构建上述 dict
    pass
```

> **改造思路**：将现有 AI-driven 的"读 JSON → 执行 → 拼报告"逻辑提取为可调用函数，
> 同时保留 skill 的 AI 交互入口不变。`run_check()` 作为程序化入口。

### 4.5 run_check.py（新增 — 调度入口，**技能内自包含**）

```python
#!/usr/bin/env python
"""
RPA 数据采集校验 — 定时调度入口（技能自包含，可被任何项目调用）

用法:
  uv run python .claude/skills/prod-rpa-checker/run_check.py
  uv run python .claude/skills/prod-rpa-checker/run_check.py --no-notify
  uv run python .claude/skills/prod-rpa-checker/run_check.py --connection test
  uv run python .claude/skills/prod-rpa-checker/run_check.py --dry-run

可移植性:
  将整个 prod-rpa-checker 目录复制到任何其他项目/智能体的 .claude/skills/ 下，
  配置好 .env 即可直接运行，无需修改任何代码。
"""

import argparse
import logging
import sys
import os
from pathlib import Path
from datetime import datetime

# ---------------------------------------------------------------------------
# 自动定位技能目录（不依赖当前工作目录，不硬编码项目路径）
# ---------------------------------------------------------------------------
SKILL_DIR = Path(__file__).resolve().parent
SCRIPTS_DIR = SKILL_DIR / "scripts"
OUTPUT_DIR = SKILL_DIR / "output"

sys.path.insert(0, str(SCRIPTS_DIR))

# 加载 .env（优先项目根目录，其次技能目录）
from dotenv import load_dotenv
# 尝试从当前工作目录往上找 .env
cwd = Path.cwd()
for parent in [cwd] + list(cwd.parents):
    env_file = parent / ".env"
    if env_file.exists():
        load_dotenv(env_file)
        break
else:
    # 兜底：技能目录下的 .env
    load_dotenv(SKILL_DIR / ".env")

# 加载 config.yaml
import yaml
CONFIG_FILE = SKILL_DIR / "config.yaml"
if CONFIG_FILE.exists():
    with open(CONFIG_FILE, "r", encoding="utf-8") as f:
        config = yaml.safe_load(f)
else:
    config = {}

# ---------------------------------------------------------------------------
# 日志配置
# ---------------------------------------------------------------------------
log_cfg = config.get("logging", {})
OUTPUT_DIR.mkdir(exist_ok=True)

logging.basicConfig(
    level=getattr(logging, log_cfg.get("level", "INFO")),
    format="%(asctime)s [%(levelname)s] %(message)s",
    handlers=[
        logging.StreamHandler(sys.stdout),
        logging.FileHandler(
            SKILL_DIR / log_cfg.get("file", "output/run_check.log"),
            encoding="utf-8",
        ),
    ],
)

# ---------------------------------------------------------------------------
# 导入技能模块
# ---------------------------------------------------------------------------
from checker import run_check
from feishu_notifier import send_to_feishu
from wiki_updater import get_tenant_token, update_wiki_content


def main():
    parser = argparse.ArgumentParser(
        description="RPA 数据采集校验定时调度（技能自包含）"
    )
    parser.add_argument(
        "--connection",
        default=config.get("checker", {}).get("default_connection", "prod"),
        help="数据库连接 (prod/test)",
    )
    parser.add_argument("--no-notify", action="store_true", help="不发送飞书通知")
    parser.add_argument("--dry-run", action="store_true", help="模拟运行")
    parser.add_argument(
        "--notify-mode",
        choices=["always", "on-failure"],
        default=config.get("notify", {}).get("mode", "always"),
        help="通知策略: always=每天都发, on-failure=仅异常时发",
    )
    args = parser.parse_args()

    notify_cfg = config.get("notify", {})
    feishu_cfg = config.get("feishu", {})

    logging.info(f"=== RPA 数据采集校验开始 (env={args.connection}) ===")

    if args.dry_run:
        logging.info("[DRY RUN] 模拟执行，跳过实际校验")
        sys.exit(0)

    # 1. 执行校验
    try:
        result = run_check(connection=args.connection)
    except Exception as e:
        # 校验异常 → 发送告警
        logging.exception("校验执行异常")
        webhook_url = os.getenv("FEISHU_WEBHOOK_URL")
        if webhook_url:
            secret = os.getenv("FEISHU_WEBHOOK_SECRET")
            send_to_feishu({
                "exec_time": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),
                "pass_count": 0,
                "fail_count": 0,
                "centers": [{
                    "name": "⚠️ 校验执行异常",
                    "all_pass": False,
                    "failures": [{"data_type": "系统", "message": str(e)}]
                }]
            }, webhook_url, secret,
               wiki_url=feishu_cfg.get("wiki_url"),
               button_text=feishu_cfg.get("button_text", "📄 查看完整报告"))
        sys.exit(1)

    logging.info(
        f"校验完成: ✅ {result['pass_count']} / ❌ {result['fail_count']}, "
        f"报告: {result['report_file']}"
    )

    # 2. 通知策略
    if args.no_notify:
        logging.info("跳过飞书通知 (--no-notify)")
        sys.exit(0)

    if args.notify_mode == "on-failure" and result["fail_count"] == 0:
        logging.info("全部通过，跳过飞书通知（on-failure 模式）")
        sys.exit(0)

    # 3. 推送飞书
    webhook_url = os.getenv("FEISHU_WEBHOOK_URL")
    if not webhook_url:
        logging.error("未配置 FEISHU_WEBHOOK_URL，跳过推送")
        sys.exit(1)

    secret = os.getenv("FEISHU_WEBHOOK_SECRET")
    ok = send_to_feishu(
        result, webhook_url, secret,
        wiki_url=feishu_cfg.get("wiki_url"),
        button_text=feishu_cfg.get("button_text", "📄 查看完整报告"),
        max_retries=notify_cfg.get("max_retries", 3),
        retry_backoff=notify_cfg.get("retry_backoff", 2),
    )

    if not ok:
        logging.error("飞书推送失败，请检查网络或 Webhook 配置")
        sys.exit(1)

    # 4. 创建新文档并挂载到父 wiki 节点下，更新卡片链接
    app_id = os.getenv("FEISHU_APP_ID")
    app_secret = os.getenv("FEISHU_APP_SECRET")
    if app_id and app_secret and result.get("report_file"):
        try:
            tenant_token = get_tenant_token(app_id, app_secret)
            new_url = update_wiki_content(
                report_file=result["report_file"],
                tenant_token=tenant_token,
                space_id=feishu_cfg.get("wiki_space_id"),
                parent_node_token=feishu_cfg.get("wiki_parent_node_token"),
                old_doc_id=feishu_cfg.get("last_doc_id"),
            )
            if new_url:
                logging.info("飞书卡片链接: %s", new_url)
        except Exception:
            logging.exception("wiki 更新失败，使用兜底链接")

    logging.info("=== RPA 数据采集校验完成 ===")


if __name__ == "__main__":
    main()
```

---

## 5. 调度配置（Windows 任务计划程序）

### 5.1 设计思路：与项目解耦

Windows 任务计划程序直接调用技能内的 `run_check.py`，**不依赖项目目录**。
技能可以放在任何位置，只需要告诉任务计划程序 `run_check.py` 的完整路径即可。

```
调度层（Windows 任务计划）
  │
  │  不关心项目结构，只知道:
  │  1. uv 命令路径
  │  2. run_check.py 的绝对路径
  │  3. .env 文件所在目录（起始目录）
  │
  ▼
技能目录（自包含）
  ├── setup_task.ps1          ← 一次性注册脚本
  └── run_check.py            ← 每天自动被调用
       └─ 通过 __file__ 自动定位自身目录 → 加载 config.yaml + .env
```

### 5.2 触发器配置

| 参数     | 值                             |
| -------- | ------------------------------ |
| 触发时间 | **每天 08:30**、**17:10**      |
| 触发类型 | 每日重复（双触发器）            |
| 失败重试 | 每 5 分钟，最多 3 次           |
| 超时限制 | 30 分钟                        |
| 电源条件 | 允许使用电池运行（笔记本注意） |

### 5.3 配置步骤

#### 方式一：图形界面

1. 打开 **任务计划程序** (`Win+R` → `taskschd.msc`)
2. 右侧 → **创建基本任务** → 名称：`RPA数据校验日报`
3. 触发器 → **每天** → 时间：**08:30**（创建后再添加第二个触发器 **17:10**）
4. 操作 → **启动程序**：
   | 字段 | 值 |
   |------|-----|
   | 程序或脚本 | `wscript.exe` |
   | 添加参数 | `E:\AI\pythonProject\aiAutoTest\.claude\skills\prod-rpa-checker\run_check_silent.vbs` |
   | 起始于 | `e:\AI\pythonProject\aiAutoTest` |

   > **起始于** 很重要：它决定了 `.env` 的查找起点。
   > 设为项目根目录，`run_check.py` 会自动找到 `.env`。

5. 条件 → 取消勾选"只有在计算机使用交流电源时才启动此任务"
6. 设置 → 勾选"如果任务失败，按以下频率重新启动" → 每 5 分钟，最多 3 次

#### 方式二：一键注册脚本（推荐）

脚本位于技能目录根：`setup_task.ps1`，参数通过命令行传入，**无需修改脚本内容**。

```powershell
cd e:\AI\pythonProject\aiAutoTest\.claude\skills\prod-rpa-checker
.\setup_task.ps1 -ProjectDir "e:\AI\pythonProject\aiAutoTest" -At "17:10" -At2 "08:30" -TaskName "RPA Data Check Daily"
```

> **参数说明**：
>
> - `-ProjectDir`：项目根目录（`.env` 所在位置），**必传**
> - `-At`：第一个触发时间，默认 `"17:10"`
> - `-At2`：第二个触发时间（可选），传值则创建双触发器
> - `-TaskName`：任务名称，默认 `"RPA数据校验日报"`

### 5.4 手动触发测试

注册后可通过以下命令立即触发一次校验（无需等待下次触发时间）：

```powershell
Start-ScheduledTask -TaskName "RPA Data Check Daily"
```

### 5.5 技能移植指南

将整个 `prod-rpa-checker` 目录复制到新智能体/项目后，**只需 3 步**：

```
Step 1: 复制目录
  cp -r prod-rpa-checker <新项目>/.claude/skills/

Step 2: 配置 .env（新项目根目录）
  FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxx
  FEISHU_WEBHOOK_SECRET=SECxxx

Step 3: 一键注册（只需改路径，无需改脚本）
  cd <新项目>/.claude/skills/prod-rpa-checker
  .\setup_task.ps1 -ProjectDir "<新项目根目录>"
```

**无需修改任何 Python 代码或脚本内容** — 参数通过命令行传入，`run_check.py` 通过 `__file__` 自动定位自身路径。

---

## 6. 消息策略：什么时候发？

### 6.1 方案选择

| 策略           | 说明                       | 推荐场景               |
| -------------- | -------------------------- | ---------------------- |
| **每天都发**   | 无论成功失败都推送         | 初期监控，养成查看习惯 |
| **仅异常时发** | 全部通过时不发，有失败才发 | 成熟期，减少噪音       |

通过 `config.yaml` 或 CLI 参数控制：

```yaml
# config.yaml
notify:
  mode: "always" # always | on-failure
```

```bash
# CLI 覆盖
uv run python run_check.py --notify-mode=on-failure
```

---

## 7. 错误处理与容错

```
执行校验 ── 异常 ──→ 记录日志 → 发送"执行异常"告警到飞书（红色卡片）
    │
    ├── 正常完成，有失败 ──→ 发送红色卡片（失败摘要 + wiki 链接）
    │
    └── 正常完成，全部通过 ──→ 发送绿色卡片 / 不发（取决于模式）
                                      │
推送飞书 ── 异常 ──→ 重试 3 次（2s/4s/8s 指数退避）
    │
    └── 仍失败 ──→ 记录日志到 output/run_check.log
```

---

## 8. 安全考虑

| 项目        | 措施                                           |
| ----------- | ---------------------------------------------- |
| Webhook URL | 存储在 `.env`，不提交 git；不打印到日志        |
| 签名校验    | 启用飞书 Webhook Secret，防止 URL 泄露后被滥用 |
| 数据库凭据  | 沿用现有 `.env` 方案，不硬编码                 |
| 日志脱敏    | 日志中不输出完整 Webhook URL                   |

---

## 9. 实施步骤

| 阶段       | 内容                                                            | 预计时间 | 状态 |
| ---------- | --------------------------------------------------------------- | -------- | ---- |
| **Step 1** | 飞书群创建自定义机器人，获取 Webhook URL + Secret               | 5 min    | ✅ 完成 |
| **Step 2** | 项目 `.env` 配置 `FEISHU_WEBHOOK_URL` / `FEISHU_WEBHOOK_SECRET` | 2 min    | ✅ 完成 |
| **Step 3** | 飞书开放平台创建应用，配置 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` | 10 min   | ✅ 完成 |
| **Step 4** | 开通应用权限：`docx:document`、`wiki:wiki`、`wiki:node:create` | 5 min | ✅ 完成 |
| **Step 5** | 创建 `config.yaml`（wiki 链接 + 报告历史记录）                  | 5 min    | ✅ 完成 |
| **Step 6** | 实现 `feishu_notifier.py`（卡片构建 + 发送 + 重试）             | 30 min   | ✅ 完成 |
| **Step 7** | 实现 `wiki_updater.py`（创建 wiki 子节点 + 写入内容 + 标题更新） | 1 h     | ✅ 完成 |
| **Step 8** | 改造 `checker.py`，增加 `run_check()` 结构化返回                | 1 h      | ✅ 完成 |
| **Step 9** | 实现 `run_check.py` 调度入口（含 wiki 更新 + 报告历史管理）     | 30 min   | ✅ 完成 |
| **Step 10**| `.\setup_task.ps1` 注册 Windows 任务计划（每天 08:30、17:10）  | 2 min    | ✅ 完成 |
| **Step 11**| 验证完整流程：校验 → wiki 子节点 → 飞书推送 → 旧文档清理       | 15 min   | ✅ 完成 |

---

## 10. 后续扩展

| 扩展方向     | 说明                                         |
| ------------ | -------------------------------------------- |
| **@指定人**  | 升级到飞书开放平台应用，失败时 @责任人       |
| **交互按钮** | 卡片加"重新校验"按钮，点击触发一次即时校验   |
| **历史对比** | 在卡片中展示"昨日 vs 今日"失败项变化趋势     |
| **多群推送** | 支持配置多个 Webhook，不同群推送不同维度摘要 |
| **即时告警** | 检测到连续 N 天失败时，额外发送即时告警      |

---

## 附录

### A. 飞书 Webhook 频率限制

- 每个 Webhook 每分钟最多发送 **100 条**消息
- 本方案每天仅发送 1 条，远低于限制

### B. 消息卡片文档

- [飞书消息卡片搭建工具](https://open.feishu.cn/document/server-docs/im-v1/message-card-builder)
- [自定义机器人文档](https://open.feishu.cn/document/client-docs/bot-v3/add-custom-bot)

### C. 技能可移植性检查清单

复制到新环境后，确认以下项：

- [ ] `run_check.py` 能通过 `python run_check.py --dry-run` 正常执行
- [ ] `.env` 中配置了 `FEISHU_WEBHOOK_URL`
- [ ] `config.yaml` 中 wiki_url 正确
- [ ] Windows 任务计划的"起始于"指向 `.env` 所在目录
- [ ] 手动触发任务计划，飞书能收到消息

---

## 11. 完整报告推送至飞书 Wiki（方案 C：创建新文档 + 卡片链接指向新文档）

> **背景**：校验报告为 Markdown 格式，飞书卡片有长度限制，无法直接塞入。
> 方案：每次创建新飞书文档 → 写入报告 → 挂载到父 wiki 节点下作为子页面 → 卡片链接指向该子页面。

### 11.1 完整流程

```
每次定时触发（08:30 / 17:10）
    │
    ▼
┌─ Step 1: checker.run_check()
│   └→ 执行数据校验 → 生成 .md 报告文件
│      report_file = output/rpa_check_report_20260604171030.md
│
    ▼
┌─ Step 2: wiki_updater.update_wiki_content()
│   │
│   ├─ ① 读取报告，从内容提取执行时间戳
│   │   └→ 标题格式: "RPA数据采集校验报告20260604181316"
│   │
│   ├─ ② create_wiki_child_node(space_id, parent_node_token, title)
│   │   └→ POST /wiki/v2/spaces/{space_id}/nodes
│   │      飞书自动生成新文档，返回: new_node_token
│   │      node_type="origin", obj_type="docx"
│   │
│   ├─ ③ _get_wiki_node_obj_token(new_node_token)
│   │   └→ GET /wiki/v2/spaces/get_node
│   │      获取飞书自动生成的 document_id (obj_token)
│   │
│   ├─ ④ write_content(actual_doc_id, blocks, tenant_token)
│   │   └→ 使用 Convert API + 创建嵌套块 API 实现原生排版
│   │      1. 调用 convert API 将 Markdown 转为块结构
│   │      2. 清洗数据（移除 parent_id、merge_info、cells 等）
│   │      3. 按子树分组分批（blocks > 1000 时，batch_size=1000）
│   │      4. 逐批调用 descendant API 写入文档
│   │
│   ├─ ⑤ 输出 wiki 链接（标题在 Step 2 创建 wiki 节点时已设置）
│   │   └→ wiki 继承空间权限，无需单独开放权限
│   │
│   └─ 返回: (new_wiki_url, new_doc_id, title)
│
    ▼
┌─ Step 3: 更新报告历史记录（独立 JSON 文件）
│   │
│   ├─ 追加新记录到 report_history 列表
│   │   └─ 记录格式: {"doc_id": obj_token, "node_token": URL短token, "title": 标题}
│   ├─ 当记录数 > 10 时，删除最早的记录
│   │   └→ DELETE /wiki/v2/spaces/{space_id}/nodes/{obj_token}
│   │      Body: {"obj_type": "docx"}
│   │      注意: 必须使用 obj_token（文档对象 token），非 node_token
│   └─ 写入 output/report_history.json
│
    ▼
┌─ Step 4: feishu_notifier.send_to_feishu(wiki_url=new_wiki_url)
│   └→ 飞书卡片按钮链接 → 新报告（wiki 子页面）
│
    ▼
  结果：
  ├─ https://wbenergy.feishu.cn/wiki/APocwMagEigGtmkgLKVcHr2Dned  ← 父页面（不变）
  │   ├── RPA数据采集校验报告20260604171030  ← 本次新报告（子页面）
  │   └── RPA数据采集校验报告20260603171030  ← 上次报告（子页面）
  └─ 旧文档超过 10 条时自动移入回收站
```

### 11.2 报告在飞书中的位置

```
飞书 wiki 知识库
└── APocwMagEigGtmkgLKVcHr2Dned（父页面，不变）
    ├── RPA数据采集校验报告20260604171030（子页面，本次）
    ├── RPA数据采集校验报告20260603171030（子页面，上次）
    └── ...
```

用户收到飞书卡片后，点击"📄 查看完整报告"按钮，直接打开最新的 wiki 子页面。

### 11.3 API 验证结果（标准 Markdown 原生渲染方案）

| 步骤 | API | 状态 |
|------|-----|------|
| Markdown 转块 | `POST /docx/v1/documents/blocks/convert` | ✅ 已调通（1000~1077 个块，含表格） |
| 创建 wiki 子节点 | `POST /wiki/v2/spaces/{space_id}/nodes` | ✅ 已调通（自动绑定空 docx） |
| 获取 document_id | `GET /wiki/v2/spaces/get_node` | ✅ 已调通 |
| 获取根块 ID | `GET /docx/v1/documents/{id}/blocks` | ✅ 已调通 |
| 写入原生内容 | `POST /docx/v1/documents/{id}/blocks/{id}/descendant` | ✅ 已调通（支持原生表格，含分批写入） |

### 11.4 报告写入逻辑说明

**核心方案**：采用 **Convert API** + **创建嵌套块 API (Descendant)** 实现标准 Markdown 格式写入。

1.  **数据转换**：
    -   调用 `convert API` 获取 Markdown 的原始块结构（`blocks`, `first_level_block_ids`）。
    -   **关键清理**：删除 `parent_id`，删除 `table.cells`，**必须删除 `table.property.merge_info`**（否则 API 报错 `invalid param`）。
    -   确保每个块有 `children` 字段（没有则补 `[]`）。
2.  **分批策略**（blocks > 1000 时自动触发）：
    -   飞书 API 限制 `descendants` 最大 1000 个。
    -   **按顶层块子树分组切分**：计算每个 first_level_block 的子树大小，按 900 阈值分组。
    -   每个批次的 `children_id` = 该批次的顶层块 ID 列表，`descendants` = 完整子树集合。
    -   ~~❌ 已废弃~~：按 descendants 数组顺序切分 + 全量 children_id → code 1770041 open schema mismatch。
3.  **内容写入**：
    -   创建 wiki 子节点获取 `document_id` 和 `root_block_id`。
    -   逐批发送给 `/blocks/{root_id}/descendant`（批次间间隔 1s 避免限流）。
    -   飞书服务端自动渲染出**原生表格、标题、列表**等排版。

### 11.5 涉及脚本

- **wiki_updater.py**: 负责完整的 wiki 内容更新流程（创建 wiki 子节点 -> Convert API -> prepare_batches() 清洗和分批 -> 逐批写入）。
- **publish_report_to_wiki.py**: 端到端全流程验证（从 Markdown 报告 → Convert → 清洗 → 分批 → 创建 wiki 子页面 → 写入）。

### 11.6 wiki_updater.py 接口设计

```python
def get_tenant_token(app_id, app_secret) -> str
def call_convert_api(markdown_content, tenant_token) -> dict
def prepare_batches(convert_data, batch_size=1000) -> list[dict]  # 按子树分组切分
def write_content_via_descendant(document_id, root_block_id, batches, tenant_token) -> bool
def create_wiki_child_node(space_id, parent_node_token, title, tenant_token) -> str
def _get_wiki_node_obj_token(node_token, tenant_token) -> str
def _get_root_block_id(document_id, tenant_token) -> str
def _update_document_title(document_id, title, tenant_token) -> None
def update_wiki_content(report_file, tenant_token, space_id, parent_node_token) -> (url, doc_id, title)
```

### 11.7 错误处理矩阵

| 场景 | 行为 | 用户感知 |
|------|------|----------|
| wiki_updater 全部成功 | 卡片链接 → 新报告子页面 | 正常 |
| 创建节点成功，写入失败 | 卡片链接 → 兜底 wiki_url | 能看到父页面 |
| 分批写入部分批次失败 | 停止后续批次，返回失败 | 文档内容不完整 |
| blocks > 1000 且子树无法完整分组 | 单个子树 > 1000 无法写入 | 返回错误 |
| 旧文档删除失败 | 卡片链接 → 新报告子页面，旧文档保留 | 正常 |
| wiki_updater 完全失败 | 卡片链接 → 兜底 wiki_url | 能看到父页面 |

### 11.8 所需权限

| 权限 | 用途 | 状态 |
|------|------|------|
| `docx:document` | 创建/写入文档、更新标题 | ✅ 已开通 |
| `wiki:wiki` | 创建 wiki 子节点、读取节点信息 | ✅ 已开通 |
| **`wiki:node:create`** | **删除 wiki 节点**（创建和删除共用此 scope） | ✅ 已开通 |

### 11.9 报告历史管理

| 策略 | 行为 |
|------|------|
| **保留阈值** | 最近 10 条报告 |
| **触发清理** | `report_history` 超过 10 条时，删除最早的 1 条 |
| **清理方式** | `DELETE /wiki/v2/spaces/{space_id}/nodes/{obj_token}` + `json={"obj_type": "docx"}` |
| **关键说明** | 必须使用 **obj_token**（文档对象 token），而非 node_token（URL 短 token）。`create_wiki_child_node` 返回的 `node_token` 是 URL 短 token，需通过 `get_node` API 获取对应的 `obj_token`。`update_wiki_content` 返回值中的 `doc_id` 就是 `obj_token`，可直接用于删除 |
| **历史记录** | 存储在 `output/report_history.json` 独立文件中（避免 config.yaml 频繁变更），每次运行自动追加/清理 |

### 11.10 分批写入验证记录

| 日期 | 报告 blocks 数 | batch_size | 策略 | 结果 |
|------|---------------|------------|------|------|
| 2026-06-06 | 1000 | 900 | ~~按数组顺序切分，children_id 全量~~ | ❌ code 1770041 schema mismatch |
| 2026-06-06 | 1077 | 900 | 按子树分组切分（851+226） | ✅ code=0 |
| 2026-06-06 | 1000 | **1000** | 按子树分组，单批全量 | ✅ code=0 |
| 2026-06-06 | 1077 | **1000** | 按子树分组（1000+77） | ✅ code=0 |

**结论**：batch_size 设为 1000（API 硬性上限）。
- ≤ 1000 blocks → 单批次，不走分批
- > 1000 blocks → 按顶层块子树分组，每批 ≤ 1000
- ~~旧版策略（900+全量 children_id）~~ 已废弃，不再尝试。

---

## 12. 架构修复与性能优化记录（V1.1）

> 更新日期：2026-06-06

### 12.1 架构问题修复

| 序号 | 问题 | 修复方式 | 涉及文件 |
|------|------|---------|---------|
| 1 | `_format_date_ranges` 代码重复 | 统一到 `report_builder.py`，`data_validator.py` 导入共享 | report_builder.py, data_validator.py |
| 2 | `sys.path.insert` 裸名导入冲突 | 改为 `sys.path.insert(SKILL_DIR)` + `from scripts.xxx import` | run_check.py, data_validator.py |
| 3 | `_ReconnectableConn` 手动 `__enter__` 资源泄漏 | 实现完整 `__enter__/__exit__` + `create_connection()` 独立函数 | data_validator.py, db_executor.py |
| 4 | `.env`/`config.yaml` 模块顶层加载 | 延迟加载：`_ensure_env_loaded()` / `_load_config()` | run_check.py |
| 5 | `exec_time` 两次 `datetime.now()` 不一致 | 统一生成一次，作为参数传递 | run_check.py, data_validator.py, report_builder.py |
| 6 | 硬编码业务逻辑违反开闭原则 | `_DATA_TYPE_SQL_MAPPING` 扩展为结构化 dict（含 `check_volume`） | data_validator.py |
| 7 | SQL 模板 `.replace()` 注入风险 | 新增 `_validate_sql_param()` 正则白名单校验 | data_validator.py |
| 8 | `wiki_updater.py` 裸 `except:` | 改为 `except Exception:` | wiki_updater.py |
| 9 | `backup_V1.0_final_release` 在版本控制中 | `.gitignore` 添加 `backup_*/` | .gitignore |
| 10 | `import argparse` 顶层死代码 | 移到 `main()` 函数内 | data_validator.py |
| 11 | `report_builder.py` 未用参数 + `outfile` 无 flush | 移除无用参数 + `outfile.flush()` | report_builder.py |

### 12.2 DB 查询并行化

- **问题**：13 个交易中心串行查询，耗时 ~9s
- **方案**：`ThreadPoolExecutor` 按交易中心并行（每线程独立数据库连接）
- **效果**：4 线程并行，DB 耗时从 9s → 3.7s（↓57%）
- **配置**：`run_check(max_workers=4)`，CLI 可通过 `--max-workers` 调整

### 12.3 性能监控日志

每次运行输出各阶段耗时：
```
[性能] DB 校验耗时: 3.7s
[性能] Wiki 更新耗时: 26.6s
[性能] 飞书推送耗时: 3.0s
[性能] 总耗时: 33.2s
```

### 12.4 旧文档删除 API 修复

- **问题**：原用 `DELETE /drive/v1/files` + `drive_file_token`，但 wiki 的 `obj_token` ≠ `drive_file_token`，永久 404
- **根因**：飞书 Wiki 节点有 `node_token`（URL 短 token）和 `obj_token`（文档对象 token），删除节点必须用 `obj_token`
- **修复**：改为 `DELETE /wiki/v2/spaces/{space_id}/nodes/{obj_token}` + `json={"obj_type": "docx"}`
- **权限**：需要 `wiki:node:create` scope（创建和删除共用）
- **验证**：2026-06-06 实测通过，返回 `{"code": 0, "msg": "success"}`

